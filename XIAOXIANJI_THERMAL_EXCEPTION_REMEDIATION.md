# 校鮮集三溫層無人取貨異常補救處理機制

**文件版本：**v0.1，對應校鮮集 v0.7 展示原型

**文件日期：**2026-09-09

**適用範圍：**加熱／保溫、常溫、冷藏三種溫層的智販機、取物櫃或其他無人取貨設備展示流程。

**資料性質：**本文件描述展示原型中的事件模型、人工處理路徑與 fail-closed 原則。設備型號、開門 API、溫控標準、物流責任、LINE 官方帳號、通知對象與正式 SLA 均為 `[TODO: 待人工確認]`。

> **核心原則：**開門結果不明、物品是否取出不明、溫層條件不明或事件重複時，系統不得把畫面動畫視為成功，不得自動完成取貨，不得重複開門，也不得在未經人工確認前自動釋放或再次扣減庫存。

## 一、異常處理的管理目標

三溫層取貨流程的補救機制需要同時保護四個對象：會員、合作社／校點窗口、庫存帳與設備維運人員。補救流程不是單純顯示錯誤訊息，而是要將不確定狀態轉成可追蹤的人工任務。

| 管理目標 | 原型中的處理方式 |
|---|---|
| 避免錯誤完成 | 只在訂單、設備、格位、取貨結果與關門回報均明確時，才顯示取貨完成。 |
| 避免重複開門 | 每次開門請求都使用展示用冪等鍵；結果不明時禁止前端再次送出相同操作。 |
| 保護庫存帳 | 將「庫存預留」、「庫存扣減」、「實體取出確認」分開展示，異常時保留待核對狀態。 |
| 保護食品與溫層 | 加熱／保溫與冷藏狀態不明時，先進入溫層人工檢查，不自動重新加熱、重新上架或交付。 |
| 讓會員知道下一步 | 以遮罩會員識別與訂單參照顯示 LINE 通知概念，不在通知內容暴露不必要的個人資料。 |
| 讓管理者可接手 | 建立設備、合作社、校點與訂單範圍的人工任務，並保留 UTC 稽核事件。 |

## 二、共用狀態模型

### 2.1 正常取貨狀態

```text
READY
  → CODE_VERIFIED
  → OPEN_REQUESTED
  → OPEN_CONFIRMED
  → ITEM_RETRIEVED
  → DOOR_CLOSED_CONFIRMED
  → PICKUP_COMPLETED
  → AUDIT_RECORDED
```

### 2.2 異常狀態

```text
OPEN_REQUESTED
  → OPEN_FAILED
  → FAIL_CLOSED
  → MANUAL_TASK_CREATED
  → LINE_NOTICE_QUEUED
  → HUMAN_REVIEW
  → RESOLVED / ESCALATED
```

```text
OPEN_CONFIRMED
  → ITEM_NOT_RETRIEVED
  → PICKUP_UNCONFIRMED
  → HOLD_INVENTORY_RECONCILIATION
  → HUMAN_REVIEW
  → RESOLVED / ESCALATED
```

```text
OPEN_REQUESTED
  → OPEN_RESULT_UNKNOWN
  → DOOR_STATE_UNKNOWN
  → NO_RETRY_UNTIL_DEVICE_CHECK
  → HUMAN_REVIEW
  → RESOLVED / ESCALATED
```

### 2.3 狀態與允許操作

| 狀態 | 會員端顯示 | 庫存處理 | 設備操作 | 管理者可做的事 |
|---|---|---|---|---|
| `OPEN_FAILED` | 取貨設備暫時無法開啟 | 保留預留，暫不完成實體取出 | 禁止自動重試 | 查看錯誤、建立替代取貨任務 |
| `OPEN_RESULT_UNKNOWN` | 正在確認設備狀態 | 保留預留，凍結自動變更 | 禁止再次開門 | 要求設備狀態查詢或現場確認 |
| `ITEM_NOT_RETRIEVED` | 已偵測開門，但尚未確認取出 | 進入實體盤點待核對 | 不再次開門 | 聯絡會員、確認格位、盤點物品 |
| `TEMPERATURE_UNKNOWN` | 溫層條件待確認 | 冷藏／加熱商品暫停交付 | 禁止自動開門或重新加熱 | 檢查溫度紀錄與食品處理決策 |
| `DOOR_NOT_CLOSED` | 取貨流程尚未完成 | 保留預留，標記設備風險 | 建立設備檢查任務 | 確認關門、通知維運與窗口 |
| `PICKUP_UNCONFIRMED` | 取貨結果待人工確認 | 不自動轉為已完成 | 維持設備鎖定或待檢查 | 依證據選擇完成、重配或取消 |
| `RESOLVED` | 顯示最終處理結果 | 依人工決策寫入帳本 | 依設備結果更新狀態 | 查看稽核與處理摘要 |

## 三、共用補救原則

### 3.1 Fail-closed

以下任一情況出現時，流程必須停住：

- 開門 API 回傳明確失敗。
- 開門 API 逾時，且無法確認設備是否已收到指令。
- 設備回報與平台事件的 `event_id` 或冪等鍵不一致。
- 訂單、校點、格位、取貨碼或溫層不一致。
- 門已開啟，但門磁、重量、光柵或設備回報無法確認物品是否取出。
- 門未關閉，或關門回報逾時。
- 冷藏或加熱／保溫條件不明。
- 通知已排程但未取得送達結果。

fail-closed 的具體結果是：**不標記 `PICKUP_COMPLETED`、不重複開門、不自動釋放庫存預留、不自動再次扣減庫存、不自動判定會員已取貨。**

### 3.2 冪等與重試

前端按鈕不可直接決定重試。正式系統應由設備 Adapter 依 `idempotency_key`、設備回報與稽核索引判定是否允許查詢或重試。展示頁只模擬這個邊界，不連接真實設備。

| 情況 | 是否自動重試 | 處理方式 |
|---|---:|---|
| UI 點擊未收到本地回應 | 否 | 顯示處理中，等待事件狀態，不再次開門。 |
| Adapter 明確回覆可安全重試 | 需人工／正式規則確認 | 先查詢原事件，再由受權流程重試。 |
| 開門結果逾時 | 否 | 標記 `OPEN_RESULT_UNKNOWN`，建立設備檢查任務。 |
| 會員重複掃描同一取貨碼 | 否 | 顯示取貨碼處理中或已使用，轉人工核對。 |
| 通知送達失敗 | 不重複開門 | 只重試通知 Adapter，保留原取貨狀態。 |

### 3.3 庫存帳分層

展示與正式資料模型都應區分以下三種數量，避免「扣庫存」與「物品已被會員取走」混為一談：

| 層級 | 說明 | 異常時的狀態 |
|---|---|---|
| 可售數量 | 可讓會員下單的數量 | 依訂單建立而減少或轉預留。 |
| 預留數量 | 已分配給訂單但尚未完成取貨的數量 | 異常時保留，直到人工決定釋放、轉配或報損。 |
| 實體取出確認 | 設備或人工確認物品已被取走的數量 | 未確認時不得標記已完成；需進入盤點。 |

## 四、開門失敗補救流程

### 4.1 適用情境

開門失敗包含設備拒絕開門、格位鎖定、網路中斷、憑證／簽章錯誤、設備離線、門鎖故障或 API 明確回傳錯誤。具體錯誤碼與設備訊息為 `[TODO: 待人工確認]`。

### 4.2 事件序列

| 順序 | 展示事件 | 系統動作 | 是否完成取貨 |
|---:|---|---|---:|
| 1 | `locker.open.requested` | 建立唯一開門請求與稽核索引 | 否 |
| 2 | `locker.open.failed` | 保存錯誤類型、設備與格位參照 | 否 |
| 3 | `pickup.fail_closed` | 凍結自動完成、禁止前端再次開門 | 否 |
| 4 | `remediation.task.created` | 建立設備檢查／替代取貨任務 | 否 |
| 5 | `line.notification.queued` | 通知會員與合作社窗口，內容使用遮罩資料 | 否 |
| 6 | `human.review.required` | 進入人工處理佇列，設定優先等級 | 否 |
| 7 | `audit.log.appended` | 寫入處理結果與 UTC 時間 | 依人工結果 |

### 4.3 會員端處理

會員端不應只顯示「錯誤」。建議顯示：

> 取貨設備目前無法開啟，訂單仍在保留處理中。請不要重複掃描。合作社窗口將確認替代取貨方式或後續處理。

若管理者已確認替代取貨點，通知可更新為：

> 原取貨設備暫時無法開啟，您的訂單參照為 `#OA-2048`。請依合作社窗口提供的替代取貨點完成後續處理。

替代取貨點、時間與聯絡人為 `[TODO: 待人工確認]`，展示頁不可填入虛構地址或電話。

### 4.4 管理者處理選項

人工窗口完成設備狀態查詢後，只能選擇一個有證據支持的處理結果：

| 處理結果 | 適用條件 | 後續動作 |
|---|---|---|
| `RETRY_AFTER_DEVICE_CHECK` | 原請求確定未送達，且設備已恢復 | 產生新的受控請求，保留原事件，不覆寫原紀錄。 |
| `ROUTE_TO_ALTERNATE_PICKUP` | 原設備無法服務，但商品仍可交付 | 建立替代取貨任務，更新會員通知與校點資料。 |
| `HOLD_FOR_OPERATOR_HANDOFF` | 需由合作社窗口現場交付 | 保留庫存預留，記錄交接人與時間。 |
| `CANCEL_AND_RELEASE` | 商品未交付且權責人員確認取消 | 依正式規則釋放預留，產生取消與庫存事件。 |
| `LOSS_OR_DAMAGE_REVIEW` | 商品或設備狀態涉及損耗 | 進入報損／責任核對，不由展示頁自動決定。 |

## 五、物品未取出補救流程

### 5.1 適用情境

物品未取出是指設備明確回報門已開，但未取得物品取出證據，或會員表示未拿到商品。可能原因包括會員未在時間內取出、格位內無物品、商品卡在格位、取錯格位、門已開但門磁／重量回報缺失，或設備感測器狀態不明。

### 5.2 事件序列

| 順序 | 展示事件 | 系統動作 | 是否完成取貨 |
|---:|---|---|---:|
| 1 | `locker.open.confirmed` | 紀錄設備明確回報門已開 | 否 |
| 2 | `pickup.item.not_retrieved` | 標記物品取出未確認 | 否 |
| 3 | `pickup.unconfirmed` | 凍結完成狀態與自動結案 | 否 |
| 4 | `inventory.reconciliation.hold` | 將預留數量轉為實體盤點待核對 | 否 |
| 5 | `member.contact.task.created` | 建立會員聯絡或窗口處理任務 | 否 |
| 6 | `line.notification.queued` | 通知會員「尚未確認完成」，避免誤以為已結案 | 否 |
| 7 | `human.review.required` | 等待設備、窗口或會員證據 | 否 |
| 8 | `audit.log.appended` | 寫入最終人工判定與證據參照 | 依人工結果 |

### 5.3 會員端處理

會員端應清楚區分「門已開」與「物品已取出」：

> 設備已回報開門，但目前尚未確認物品是否取出。請先確認格位內是否有商品，不要重複使用取貨碼；如未取到，請依 LINE 內的人工協助入口回報。

系統不得把開門成功直接轉成「已取貨」。

### 5.4 管理者核對順序

1. 查詢原始開門事件及 `idempotency_key`。
2. 查詢設備的門磁、重量、光柵或其他可用回報。正式感測器能力為 `[TODO: 待人工確認]`。
3. 核對格位商品、批次、校點與訂單。
4. 取得會員回報或合作社窗口的人工確認。
5. 若仍不明，安排現場盤點，不自動釋放庫存或完成訂單。
6. 根據證據選擇完成、替代交付、取消、重配或損耗核對。
7. 將人工處理者、時間、證據參照與結果寫入稽核紀錄。

## 六、三種溫層的差異化補救

### 6.1 加熱／保溫

加熱／保溫流程的重點是「不自動推論食品仍適合交付」。

| 異常 | 補救處理 |
|---|---|
| 開門失敗 | 保留預留，建立設備檢查或替代交付任務；不自動重新加熱。 |
| 門已開但物品未取出 | 暫停完成，確認保溫／加熱狀態與食品處理規則，再決定交付或報損。 |
| 溫度回報不明 | `TEMPERATURE_UNKNOWN`；禁止自動開門、重新加熱或標記完成。 |
| 門未關閉 | 建立設備與食品安全檢查任務，通知窗口，不自動完成。 |

實際溫度門檻、保溫時間、加熱方式與食品處理責任均為 `[TODO: 待人工確認]`。展示頁只呈現狀態與人工決策點。

### 6.2 常溫

常溫流程的重點是「校點、批次與格位一致」。

| 異常 | 補救處理 |
|---|---|
| 開門失敗 | 維持預留，確認設備後提供替代窗口或替代設備。 |
| 門已開但物品未取出 | 盤點格位，確認是否仍在原格位，不自動重複開門。 |
| 格位盤點少件 | 將 SKU 與批次轉入庫存盤點任務，凍結相關訂單。 |
| 取貨碼重複使用 | 顯示處理中或已使用，交由窗口查詢原事件。 |

### 6.3 冷藏

冷藏流程的重點是「冷藏條件與交付責任需可追溯」。

| 異常 | 補救處理 |
|---|---|
| 開門失敗 | 保留預留，確認冷藏設備狀態後再決定重配或替代交付。 |
| 門已開但物品未取出 | 先確認冷藏狀態與時間線，再進行人工盤點。 |
| 冷藏條件不明 | 暫停交付與自動結案，建立冷藏檢查任務。 |
| 門未關閉 | 通知設備維運與合作社窗口，保留溫度／門磁證據。 |
| 商品疑似受溫層影響 | 進入食品處理與責任確認，不由原型自動判定可售或報損。 |

冷藏溫度門檻、記錄頻率、保存時間與食品責任界線均為 `[TODO: 待人工確認]`。

## 七、LINE 通知與人工任務

### 7.1 通知層級

| 層級 | 接收者 | 通知目的 |
|---|---|---|
| 會員提示 | 遮罩會員對應的 LINE 服務入口 | 告知取貨未完成或需要人工協助，不暴露內部錯誤細節。 |
| 合作社窗口 | 該合作社授權窗口 | 確認訂單、校點、商品與會員後續處理。 |
| 設備維運 | 經授權的設備維運群組 | 處理設備離線、門鎖、感測器或溫層問題。 |
| 管理者升級 | 企業管理者或值班責任人 | 處理逾時、重複異常或高風險事件。 |

正式接收者、LINE 官方帳號、訊息模板、升級時間與通知頻率為 `[TODO: 待人工確認]`。

### 7.2 通知內容最小化

通知應以 `order_ref`、`school_ref`、`device_ref` 與事件狀態為主，不直接放會員姓名、完整電話、完整取貨碼或其他不必要 PII。完整資料應只在授權管理畫面以最小權限查看。

**會員通知示意：**

> 取貨狀態需要確認｜訂單 `#OA-2048`｜取貨點 `A`。請勿重複掃描，合作社窗口將協助處理。

**合作社窗口通知示意：**

> 待處理取貨異常｜設備 `LK-12`｜校點 `school-xindian-07`｜事件 `EXC-20260909-01`｜請進入授權管理畫面確認。

### 7.3 人工任務欄位

| 欄位 | 說明 |
|---|---|
| `task_id` | 補救任務唯一識別 |
| `company_id` | 由伺服器決定的租戶範圍 |
| `coop_id` | 合作社範圍 |
| `school_id` | 校點範圍 |
| `order_id` | 訂單參照 |
| `device_id` | 設備參照 |
| `temperature_zone` | `heated`／`ambient`／`chilled` 的正式命名待確認 |
| `exception_code` | `OPEN_FAILED`、`ITEM_NOT_RETRIEVED` 等展示代碼 |
| `priority` | `normal`、`high`、`critical` 的正式規則待確認 |
| `owner_ref` | 受權處理人員或群組的遮罩參照 |
| `status` | `OPEN`、`IN_PROGRESS`、`RESOLVED`、`ESCALATED` |
| `audit_ref` | 稽核事件參照 |
| `occurred_at_utc` | 事件發生 UTC 時間 |

## 八、展示原型的互動腳本

### 8.1 失敗情境選擇

新增展示區「三溫層異常補救中心」，提供三個溫層按鈕與四個異常情境：

- 開門失敗。
- 開門結果不明。
- 物品未取出。
- 溫層條件不明。

訪客選擇情境後，可播放以下四階段流程：

```text
異常偵測
  → Fail-closed 凍結
  → 建立人工任務／通知
  → 等待人工確認
```

訪客再按「模擬人工確認」後，才可看到「補救結果已留痕」。畫面需清楚說明這是合成資料與展示事件，不會實際開門、發送 LINE 或修改庫存。

### 8.2 展示事件

| 順序 | 展示事件 | 畫面文字 |
|---:|---|---|
| 01 | `exception.detected` | 偵測到取貨異常 |
| 02 | `pickup.fail_closed` | Fail-closed 已啟動 |
| 03 | `remediation.task.created` | 人工補救任務已建立 |
| 04 | `line.notification.queued` | LINE 通知已排入展示佇列 |
| 05 | `human.review.required` | 等待授權管理者確認 |
| 06 | `audit.log.appended` | 補救結果已寫入展示稽核 |

### 8.3 展示成功條件

只有在以下條件全部成立時，原型才顯示「補救結果已留痕」：

1. 異常代碼與設備、格位、校點、訂單參照一致。
2. Fail-closed 狀態已顯示。
3. 人工任務已建立。
4. 通知事件已排入展示佇列。
5. 訪客明確點擊「模擬人工確認」。
6. 畫面顯示 `audit.log.appended`。

這個完成狀態只代表展示流程完成，不代表真實設備、LINE、庫存、付款或食品處理已完成。

## 九、驗收清單

### 互動驗收

- [ ] 可切換加熱／保溫、常溫與冷藏。
- [ ] 可切換開門失敗、開門結果不明、物品未取出與溫層條件不明。
- [ ] 播放流程時先顯示 fail-closed，再建立任務與通知，最後等待人工確認。
- [ ] 未點擊人工確認前，不顯示補救完成。
- [ ] 開門結果不明情境明確顯示「禁止重試」。
- [ ] 物品未取出情境明確顯示「庫存盤點待核對」。
- [ ] 加熱／保溫與冷藏情境顯示溫層責任待確認。
- [ ] 重置後可重新播放，且不殘留上一個情境的狀態。
- [ ] 行動版可閱讀事件、通知與人工處理文案。

### 治理驗收

- [ ] 原型不呼叫真實設備 API、LINE API、付款 API 或資料庫。
- [ ] 外部服務均以 Adapter 概念呈現。
- [ ] 不把前端動畫當成正式取貨完成。
- [ ] 不自動重複開門、不自動釋放預留、不自動重複扣減庫存。
- [ ] 所有展示數據、設備代號、事件與狀態均為合成資料。
- [ ] 異常結果保留人工處理與稽核回寫概念。

## 十、尚待確認事項

| 類別 | 內容 |
|---|---|
| 設備 | 智販機、取物櫃、iMin F1 或其他設備的正式型號與能力 |
| API | 開門、關門、感測器、溫度、查詢、簽章、冪等與錯誤碼 |
| 溫控 | 加熱／保溫與冷藏的門檻、記錄頻率、保存時間與責任 |
| 庫存 | 預留、扣減、報損、重配、替代交付與取消的正式帳務規則 |
| 通知 | LINE 官方帳號、會員綁定、合作社窗口、維運群組與升級規則 |
| 權限 | 總公司、合作社、校點、物流與設備維運的最小權限矩陣 |
| 稽核 | 保存期限、PII 遮罩／加密、人工證據與 UTC 事件格式 |
| SLA | 開門失敗、物品未取出、溫層異常與通知失敗的服務時限 |

## References

[1]: file:///home/ubuntu/work-stallpay-lp/XIAOXIANJI_LANDING_AND_THERMAL_PICKUP_PLAN.md "校鮮集首頁與三溫層取貨規劃"

[2]: https://feat-xiaoxianji-enterprise-p.stallpay-lp.pages.dev/?v=07 "校鮮集 Prototype v0.7 Cloudflare Branch Preview"

[3]: https://oss-sg.imin.sg/docs/en/SDK.html "iMin 官方 SDK 文件"

[4]: https://oss-sg.imin.sg/docs/en/Printer.html "iMin 官方印表機文件"

本文件以 [1] 的三溫層狀態機與 Adapter 邊界為基礎，將開門失敗、開門結果不明、物品未取出與溫層條件不明補充為可展示、可追蹤且 fail-closed 的補救流程。正式設備與營運規則仍需由權責單位確認。
