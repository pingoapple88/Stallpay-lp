# 校鮮集 iMin F1 收銀與自動結算對帳操作說明

**文件版本：**v0.1

**適用原型：**校鮮集食品團購企業展示原型 Prototype v0.7

**文件日期：**2026-09-09

**文件用途：**提供企業管理者、合作社窗口與展示操作人員理解 iMin F1 收銀／發票模擬，以及教職員福利點數／回饋金自動結算對帳模組的操作順序、畫面狀態、事件名稱與失敗邊界。

> **重要限制：**本文件對應的是靜態展示原型。原型不呼叫 iMin、支付、發票、ERP、LINE 或其他外部 API，不建立正式訂單，不收集會員資料，也不保存付款資訊。畫面中的訂單、會員、金額、點數、回饋金與發票號碼均為合成展示資料，不具正式付款、發票、稅務、入帳或服務承諾效力。

## 一、公開展示入口與頁尾確認

公開展示網址為 [校鮮集 Cloudflare Branch Preview](https://feat-xiaoxianji-enterprise-p.stallpay-lp.pages.dev/?v=06)。本次檢查結果為 HTTP 200，回應內容已確認包含 `Prototype v0.7`、`© 2026 JCINN 捷州資訊. Concept showcase.` 與七項主權條款列。

頁尾的正式顯示文字如下：

> **© 2026 JCINN 捷州資訊. Concept showcase.**
>
> **§1 平台中立 · §2 原始碼主權 · §3 標準化部署 · §4 技術棧白名單 · §5 資料主權 · §6 AI 可替換 · §7 主權檢核**

頁尾採用圖一的 JCINN 捷州資訊主權展示格式。這個頁尾是展示網站的品牌與治理標示，不等同於正式公司名稱、食品供應商、支付服務商、發票服務商或萬佳鄉／合作社的營運責任主體。

## 二、模組總覽

| 模組 | 主要使用者 | 原型入口 | 操作結果 |
|---|---|---|---|
| iMin F1 收銀／發票 | 收銀人員、店長、展示操作人員 | 導覽列「iMin 收銀」或頁面 `#imin-cashier` | 模擬掃碼、金額確認、列印與事件回寫 |
| 自動結算對帳 | 財務人員、合作社窗口、管理者 | 導覽列「福利結算」或頁面 `#benefit-settlement` | 模擬讀單、點數計算、資格鎖定與批次寫入 |
| ERP 對帳摘要 | 企業管理者、財務人員 | 「ERP 儀表板」 | 查看應收、已核對、待核對與異常的展示摘要 |
| Order AI 成功資料流 | 管理者、產品展示人員 | 「成功資料流」 | 觀察訂單辨識至庫存與批次對帳的完整展示流程 |

## 三、開始操作前的共同原則

### 3.1 使用展示網址

1. 開啟 [校鮮集 Cloudflare Branch Preview](https://feat-xiaoxianji-enterprise-p.stallpay-lp.pages.dev/?v=06)。
2. 若瀏覽器保留舊內容，使用完整網址重新載入，或加上新的查詢參數，例如 `?v=06`。
3. 本頁是單頁展示網站。主導覽的每一個項目會將畫面定位到對應展示區段。
4. 這些按鈕只改變前端展示狀態，不會送出正式交易或通知。

### 3.2 狀態判讀

| 狀態文字 | 意義 |
|---|---|
| 待處理 | 該展示事件尚未在模擬中完成 |
| 目前 | 該展示事件是目前操作階段 |
| 已完成 | 該展示事件已在本次模擬中完成 |
| 已確認 | 展示資料已通過前一個畫面步驟，不代表外部付款成功 |
| 已回寫 | 前端展示的 Adapter 事件已完成，不代表正式硬體或後端已回寫 |
| 待人工核對 | 系統刻意保留人工決策，不自動入帳或完成 |
| fail-closed | 結果不明時停止後續高風險動作 |

### 3.3 金額與資料邊界

畫面中的 `NT$ 240`、`NT$ 220`、點數與回饋金是展示數字。正式系統應使用最小貨幣單位儲存金額，付款與庫存扣減必須分離，時間應使用 UTC，會員識別與 PII 應採遮罩、雜湊或加密。正式 API 的第一步應驗證租戶、角色與門店範圍。

## 四、iMin F1 收銀操作說明

### 4.1 模組用途

iMin F1 收銀模組用來展示收銀終端如何處理已確認團購訂單、福利點數折抵、金額確認、列印事件與狀態回寫。原型以使用者所稱「iMin F1」對應公開產品名稱「iMin Falcon 1」作為候選設備；正式採購型號仍需人工確認。

根據已查核的 iMin 公開資料，Falcon 1 公開頁面列出 80mm 熱感印表機、QR／1D／2D 掃描、NFC、Android 11 series、Wi-Fi、Bluetooth、4G、GPS 與多種週邊連接埠。官方 SDK 文件與印表機文件提供設備初始化、裝置資訊與列印整合方向。這些公開資料只說明可評估的整合方向，不代表校鮮集已取得設備權限、支付資格、發票資格或正式 SDK 接點。[1] [2] [3]

### 4.2 開啟模組

1. 在頁面上方點擊「**iMin 收銀**」。
2. 頁面會定位到「iMin F1 收銀、發票列印與訂單回寫」區段。
3. 初始畫面顯示 `iMin Falcon 1／F1 收銀終端`、`ONLINE MOCK・Printer` 與展示任務 `#OA-2048｜A-032`。
4. 初始狀態為「待結帳／掃描商品」，主要按鈕為「確認金額」，次要按鈕為「測試列印」。
5. 初始發票區顯示「發票尚未列印」與「待處理」。

### 4.3 收銀模式選擇

原型提供三種收銀模式。點擊模式後，畫面會重置為該模式的初始狀態。

| 模式 | 用途 | 初始主要操作 | 完成後文字 |
|---|---|---|---|
| 團購發票 | 展示團購訂單金額與發票列印 | 確認金額 → 列印發票 | 發票已列印 |
| 收銀收據 | 展示取貨現場的收據核對 | 確認收銀 → 列印收據 | 收據已列印 |
| 退貨沖正 | 展示原訂單關聯、沖正與留痕 | 確認沖正 → 列印沖正單 | 沖正單已列印 |

此三種模式是展示分支，不代表正式稅務流程已定義。退貨沖正仍需正式確認原訂單關聯、退款方式、福利點數回補、發票作廢／折讓與會計分錄。

### 4.4 團購發票操作步驟

#### 步驟一：確認任務與商品

畫面固定展示以下合成資料：

- 訂單：`#OA-2048`
- 取貨任務：`A-032`
- 商品：鮮乳與點心組 × 2
- 商品小計：`NT$ 240`
- 福利點數折抵：`- NT$ 20`
- 應收金額：`NT$ 220`

操作人員應先確認展示訂單、商品、會員遮罩識別與應收金額一致。正式系統還必須確認付款授權狀態、價格版本、會員資格、福利規則與稅務欄位。

#### 步驟二：點擊「確認金額」

1. 點擊主要按鈕「**確認金額**」。
2. 畫面標題由「待結帳／掃描商品」改為「**金額已確認／準備列印**」。
3. 說明文字會變為「金額、福利折抵與訂單已核對，請執行硬體列印」。
4. 收銀事件文字顯示 `cashier.total.confirmed・金額已確認`。
5. 發票狀態顯示「等待硬體列印」。
6. 發票事件狀態顯示「已確認」。
7. 主要按鈕改為「**列印發票**」。

此步驟只代表展示資料完成前端核對。它不代表已完成付款授權、扣款、發票開立或稅務申報。

#### 步驟三：點擊「列印發票」

1. 點擊主要按鈕「**列印發票**」。
2. 畫面標題改為「**收銀事件已回寫**」。
3. 畫面顯示 `printer.invoice.printed・展示完成`。
4. 發票狀態改為「**發票已列印**」。
5. 發票事件狀態改為「**已回寫**」。
6. 展示發票號碼產生為 `AB-20260909-2048`。
7. 主要按鈕改為「已完成」並停用，避免在本次模擬中重複完成。

原型中按鈕停用是前端展示行為。正式系統仍必須以設備 Adapter 回傳、印表機結果、發票服務商結果與付款查詢結果共同決定狀態。列印失敗、結果逾時或服務商回覆不明時，不可標記正式發票完成，也不可重複請款。

#### 步驟四：必要時點擊「再次列印」

原型的次要按鈕會顯示「再次列印」。點擊後只會建立一個展示列印測試提示，不會真的呼叫 iMin Printer SDK。正式系統的再次列印應先確認：

1. 原發票或收據是否已成功開立。
2. 是否允許重印，以及重印是否需要註記。
3. 是否列印正式憑證、非正式明細或取貨單。
4. 重印事件是否綁定原 `invoice_ref`、`order_id` 與操作人員。
5. 是否寫入 `audit_logs`。

#### 步驟五：點擊「重置收銀模擬」

點擊「**重置收銀模擬**」後，畫面回到待結帳狀態：

- 主要按鈕：確認金額
- 收銀事件：等待操作
- 發票狀態：發票尚未列印
- 發票事件：待處理
- 發票號碼：待產生

重置只重置瀏覽器中的展示狀態，不撤銷任何正式交易，因為本原型根本不建立正式交易。

### 4.5 收據模式操作

1. 點擊「收銀收據」模式。
2. 確認畫面標示為 `iMin Falcon 1／F1 收銀收據`。
3. 點擊「確認收銀」。
4. 確認畫面進入「金額已確認／準備列印」。
5. 點擊「列印收據」。
6. 確認狀態顯示「收據已列印」與 `printer.invoice.printed・展示完成`。

正式系統應將收據列印與付款結果、取貨完成、發票狀態分開建模，不能因收據列印成功就推論付款成功或取貨完成。

### 4.6 退貨沖正模式操作

1. 點擊「退貨沖正」模式。
2. 確認畫面顯示原單關聯概念。
3. 點擊「確認沖正」。
4. 確認畫面進入準備列印狀態。
5. 點擊「列印沖正單」。
6. 確認狀態顯示「沖正單已列印」。

原型只展示狀態切換。正式退貨流程必須經人工權限與正式 Adapter，並核對原訂單、付款、發票、庫存、福利點數與對帳批次。任何一項結果不明，都應維持待處理，不可自動關閉。

## 五、自動結算對帳操作說明

### 5.1 模組用途

「福利結算」模組展示教職員福利點數、回饋金資格、退貨沖正與合作社批次對帳的自動化流程。原型使用合成會員與合成金額，展示以下四個事件：

1. `benefit.source.orders`：讀取已完成訂單。
2. `benefit.points.calculated`：計算福利點數。
3. `benefit.rebate.locked`：鎖定回饋金資格。
4. `benefit.settlement.posted`：寫入合作社批次。

「自動」表示在正式規則、資料與權限均已核准後，系統按事件順序執行；它不表示所有會員資格或異常都可以無人審核。原型明確保留人工核對狀態。

### 5.2 開啟模組

1. 在頁面上方點擊「**福利結算**」。
2. 頁面會定位到「教職員福利點數與回饋金，結算後再入帳」。
3. 初始批次顯示 `2026-09 福利批次｜北區員生合作社`。
4. 顯示教職員工、12 校與合成會員 2,480 人。
5. 期間選單包含「2026-09 月結展示」與「2026-08 月結展示」。
6. 初始主要按鈕為「**執行自動結算**」。
7. 初始狀態為「待執行」。

### 5.3 期間選擇

1. 點擊「選擇福利結算期間」。
2. 選擇需要展示的月份。
3. 期間文字會更新為所選展示批次。
4. 期間選擇本身不會產生正式結算批次，也不會寫入資料庫。

正式系統選定期間後，應鎖定批次範圍與資料快照，包含合作社、門店／校點、會員狀態、已完成訂單、退款窗口與規則版本。期間重跑必須使用批次版本與冪等鍵，避免重複入帳。

### 5.4 執行四階段自動結算

點擊「**執行自動結算**」後，原型每約 680 毫秒推進一個展示階段。實際正式系統不應以固定時間假設外部服務完成，而應等待事件確認與重試結果。

#### 階段一：讀取已完成訂單

畫面狀態變為處理中 `1/4`。

- 事件：`benefit.source.orders`
- 顯示：讀取已完成訂單
- 目的：取得符合批次期間、合作社與校點範圍的完成訂單
- 正式檢查：訂單狀態、付款狀態、退款窗口、會員資格與資料版本

若來源資料不完整，應標記批次待核對，不得直接進入正式入帳。

#### 階段二：計算福利點數

畫面狀態變為處理中 `2/4`。

- 事件：`benefit.points.calculated`
- 目的：依核准的福利規則計算點數
- 展示點數：4,280 點
- 正式檢查：規則版本、會員身分、商品適用性、退貨與折讓

點數計算結果應寫入可追溯的福利帳本。不可只更新會員餘額而沒有來源訂單與規則版本。

#### 階段三：鎖定回饋金資格

畫面狀態變為處理中 `3/4`。

- 事件：`benefit.rebate.locked`
- 目的：鎖定符合條件的回饋金資格，等待後續人工或批次核對
- 展示待確認回饋金：NT$ 428
- 展示待核對會員：31

此階段不是把回饋金直接發放給會員。資格衝突、退貨尚未結束、來源不明或規則版本衝突時，應維持待人工核對。

#### 階段四：寫入合作社批次

畫面狀態變為批次已建立 `4/4`。

- 事件：`benefit.settlement.posted`
- 目的：把已通過批次規則的結果寫入合作社結算批次
- 展示批次：批次已建立
- 展示異常沖正：3 筆

正式系統的 `posted` 必須表示寫入正式批次帳本成功，不應只表示前端動畫完成。若資料庫、ERP 或對帳服務結果不明，應停止自動入帳並建立人工處理任務。

### 5.5 結算結果表判讀

原型列表展示三筆遮罩會員資料：

| 會員／校點 | 福利點數 | 回饋金 | 初始展示狀態 |
|---|---:|---:|---|
| `masked-staff-031`／新店國小 | 1,280 點 | NT$ 128 | 待結算 |
| `masked-staff-044`／北區聯合團 | 860 點 | NT$ 86 | 待資格核對 |
| `masked-staff-092`／桃園教職員會 | 2,140 點 | NT$ 214 | 待結算 |

在原型播放結束後，多數狀態會顯示已入批次，但第二筆仍保留人工核對概念。這是刻意示範「批次完成不等於所有會員都可自動入帳」。

### 5.6 重置展示

點擊「**重置展示**」後：

- 結算狀態回到待執行。
- 四個事件回到目前／待執行的初始排列。
- 點數、回饋金、待核對會員與異常沖正回到展示初值。
- 不會刪除或修改任何正式資料，因為原型沒有正式資料層。

## 六、iMin F1 與自動結算的整合邏輯

iMin F1 收銀與自動結算不是同一個交易動作。收銀模組負責展示單筆訂單的金額確認與列印事件；結算模組負責在批次層級整理已完成訂單、福利點數、回饋金資格與合作社對帳。

正確的正式資料流應如下：

```text
訂單確認
  → Payment Adapter 回傳明確付款結果
  → Invoice Adapter 處理正式發票結果
  → iMin Printer Adapter 回傳列印結果
  → Order／Pickup 狀態更新
  → 訂單完成事件進入福利結算來源範圍
  → 福利點數計算
  → 回饋金資格鎖定
  → 合作社批次對帳
  → 人工核對異常
```

不可採用以下不安全推論：

- 列印成功就代表付款成功。
- 收據列印成功就代表發票已開立。
- 訂單完成就代表福利回饋金已入帳。
- 前端動畫完成就代表 ERP 批次已寫入。
- 庫存扣減就代表金融付款已完成。

## 七、異常與人工處理

### 7.1 iMin F1 收銀異常

| 異常 | 原型應如何解讀 | 正式處理建議 |
|---|---|---|
| 掃碼失敗 | 展示不會真的連接掃描器 | 保留待處理，不建立不完整訂單 |
| 金額不一致 | 不可直接列印正式憑證 | 重讀價格版本或轉人工 |
| 付款結果逾時 | 不可依前端結果判定成功 | 使用 Payment Adapter 查詢並鎖定重試 |
| 印表機失敗 | 不可標示發票／收據完成 | 保留列印待重試事件 |
| 發票服務不明 | 不可顯示正式發票完成 | 建立發票查詢與人工核對任務 |
| 重複點擊 | 前端可停用按鈕，但正式仍需冪等 | 以 `idempotency_key` 阻止重複請款／列印 |

### 7.2 自動結算異常

| 異常 | 應停止的動作 | 必須保留的證據 |
|---|---|---|
| 會員資格不明 | 不自動鎖定或入帳回饋金 | 會員狀態、規則版本、人工任務 |
| 訂單退款尚未結束 | 不列入最終回饋金批次 | 原訂單、退款事件、時間 |
| 點數規則衝突 | 不寫入正式餘額 | 規則版本、計算差異 |
| ERP 寫入逾時 | 不重複建立批次 | outbox、批次鍵、重試狀態 |
| 金額對不上 | 不關閉對帳批次 | 原始來源、差異額、操作人員 |
| 事件重複 | 不重複計點或入帳 | `event_id`、冪等鍵、消費者狀態 |

## 八、正式實作的 Adapter 與治理要求

正式系統不得將 iMin SDK、支付 SDK、發票服務商或 LINE API 直接嵌入收銀與結算核心。至少應提供以下抽象介面：

| 介面 | 職責 |
|---|---|
| `IHardwareDeviceProvider` | iMin 設備初始化、序號、狀態與硬體能力 |
| `IPrinterProvider` | 取貨單、收據、發票列印事件 |
| `IPaymentProvider` | 授權、查詢、退款、取消與冪等請款 |
| `IInvoiceProvider` | 發票開立、查詢、作廢、折讓與重印 |
| `IBenefitLedgerProvider` | 福利點數帳本、回饋金資格與沖正 |
| `IReconciliationProvider` | 付款、發票、訂單、庫存與福利批次對帳 |
| `IAuditLogProvider` | 所有狀態變更、人工處理與查詢留痕 |
| `IEventBus` | 收銀、訂單、庫存、福利與對帳事件解耦 |

所有事件應包含 `company_id`、必要的 `store_id`／`coop_id`、`order_id`、`event_id`、`idempotency_key`、UTC 時間與來源系統。所有查詢應先驗證 RBAC，且帶入租戶範圍。正式金額應使用最小貨幣單位，會員 PII 應加密或遮罩。AI 若參與會員資格、商品識別或訂單整理，低信心時必須停在人工審核隊列。

## 九、操作人員驗收清單

### iMin F1 收銀

- [ ] 已開啟公開展示網址並確認版本為 Prototype v0.7。
- [ ] 已選擇正確收銀模式。
- [ ] 已核對展示訂單、商品小計、福利折抵與應收金額。
- [ ] 已點擊確認金額，看到 `cashier.total.confirmed`。
- [ ] 已點擊列印操作，看到 `printer.invoice.printed`。
- [ ] 已確認發票／收據狀態與展示事件回寫文字。
- [ ] 已理解展示發票不具正式憑證效力。
- [ ] 已知道列印成功不等於付款成功。
- [ ] 已使用重置按鈕將展示回到初始狀態。

### 自動結算對帳

- [ ] 已選擇福利結算期間。
- [ ] 已核對合作社、校點與合成會員範圍。
- [ ] 已執行自動結算。
- [ ] 已觀察四個事件依序完成。
- [ ] 已確認點數、回饋金與待核對會員數字為展示資料。
- [ ] 已辨識待人工核對與異常沖正狀態。
- [ ] 已理解 `benefit.settlement.posted` 在原型中是展示事件，不是正式入帳證據。
- [ ] 已使用重置按鈕重新演示流程。

## 十、正式上線前待確認事項

以下項目目前不能由原型推定：

| 類別 | 待確認項目 |
|---|---|
| 設備 | 正式 iMin 型號是否為 Falcon 1、Android／韌體版本、SDK 版本、設備序號管理、列印紙規格 |
| 收銀 | 掃碼來源、商品主檔、離線收銀範圍、支付結果查詢、退貨與沖正權限 |
| 發票 | 稅籍、字軌、載具、捐贈碼、發票服務商、作廢／折讓、重印規則 |
| 福利 | 合作社法律身分、會員資格、點數倍率、回饋金規則、預算、退貨沖正與入帳時間 |
| 對帳 | ERP 帳套、批次期間、差異容忍值、人工核對角色、月結與重跑政策 |
| 資安 | 設備憑證、API 簽章、網路隔離、帳號權限、稽核保存期限、PII 保留政策 |
| 維運 | 收銀、發票、設備、ERP、LINE 與福利模組的責任 owner、SLA 與故障升級路徑 |

## References

[1]: https://www.imin.com/product/falcon-1/ "iMin Falcon 1 官方產品頁"

[2]: https://oss-sg.imin.sg/docs/en/SDK.html "iMin 官方 SDK 文件"

[3]: https://oss-sg.imin.sg/docs/en/Printer.html "iMin 官方印表機文件"

[4]: https://www.imin.com/faq/ "iMin 官方 FAQ"

[5]: https://feat-xiaoxianji-enterprise-p.stallpay-lp.pages.dev/?v=06 "校鮮集 Prototype v0.7 Cloudflare Branch Preview"

[6]: file:///home/ubuntu/work-stallpay-lp/QA_BROWSER_NOTES.md "校鮮集瀏覽器與部署驗證紀錄"

本文件依據 [1] [2] [3] [4] [5] [6] 撰寫。原型操作狀態以頁面實際 JavaScript 與瀏覽器驗證紀錄為準；正式設備、付款、發票、ERP 與福利規則仍須人工確認。
