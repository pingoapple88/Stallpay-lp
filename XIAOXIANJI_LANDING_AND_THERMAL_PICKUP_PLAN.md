# 校鮮集首頁視覺與三溫層無人取貨展示規劃

**文件版本：**v0.1 草案

**文件日期：**2026-09-09

**適用名稱：**「校鮮集」目前為展示候選名稱，正式品牌名稱、公司名稱與網域為 `[TODO: 待人工確認]`。

**展示對象：**經營北部國中小教職員工食品團購的企業管理者、團購營運主管、物流／倉配主管與業務副總。

**主要 CTA：**進入團購平台。正式團購平台網址為 `[TODO: 團購平台正式網址]`，不可在展示頁自行猜測或建立虛構連結。

> 本文件將首頁視覺與文案設計，及「加熱／保溫、常溫、冷藏」三溫層無人取貨模擬整合在同一條管理者展示路徑中。所有學校數、訂單數、設備狀態、溫度數值與物流批次均應標示為「展示資料／示意數據」，不代表正式服務成果或營運承諾。[1]

## 一、品牌定位與首頁任務

### 1.1 品牌定位

「校鮮集」以校園、鮮食與集中團購為品牌聯想核心。它不是單一商品商店，而是面向企業管理者的多校食品團購營運展示平台。首頁必須先讓訪客理解「多校團購可以被集中管理」，再引導訪客進入團購平台。

品牌主張可整理為以下三個層次：

| 層次 | 主張 | 對管理者的意義 |
|---|---|---|
| 網絡 | 100 校團購總覽 | 把分散校點放在同一個管理視角 |
| 營運 | 訂單、商品、配送、取貨與對帳串聯 | 降低人工彙整與狀態落差 |
| 例外 | 缺貨、溫層、逾期、設備與對帳異常可追蹤 | 管理者優先處理需要人工決策的事項 |

「100 校」只能作為展示數據使用。首頁的角標、儀表板卡片與流程圖均應附上「展示資料／示意情境」標示。

### 1.2 首頁五秒任務

訪客在首頁首屏應能回答以下問題：

1. 這是一個管理多校食品團購的企業平台概念。
2. 平台涵蓋校點、會員、商品、訂單、配送／取貨、庫存與對帳。
3. 主要入口是「進入團購平台」。
4. 100 校與其他數字均為展示資料。
5. 無人取貨設備可以依商品溫層分流，但正式硬體與 API 尚待確認。

### 1.3 首頁操作主線

首頁不應要求訪客先閱讀完整產品介紹。建議使用以下展示主線：

```text
100 校團購總覽
  → 今日營運狀態
  → 選擇學區／學校
  → 查看團購檔期與商品
  → 查看配送／取貨與溫層
  → 進入團購平台
```

## 二、首頁 Landing Page 視覺架構

### 2.1 整體視覺方向

首頁採「企業管理中樞＋校園鮮食服務」的混合視覺。管理者需要感受到資料清楚、流程可控與例外可追蹤；品牌同時應保留校園與鮮食的親和感，避免呈現為冷硬的純 ERP 報表。

| 視覺項目 | 建議方向 |
|---|---|
| 主色 | 深靛藍／深夜藍，建立企業管理與資料可信度 |
| 輔色 | 鮮綠、冷藏藍、暖橘，用於食品狀態與流程區分 |
| 強調色 | 金色或暖黃色，用於 CTA、重要狀態與圖一格式頁尾主權文字 |
| 背景 | 淺灰白內容區搭配深色管理卡片，避免全頁單一深色 |
| 圓角 | 卡片採中等圓角，設備與流程節點可使用較小圓角 |
| 圖像語言 | 校園平面節點、配送批次、溫層標籤、設備狀態與簡化資料流 |
| 動態 | 只在切換校點、播放資料流、開門模擬與狀態回寫時使用短動畫 |

正式品牌色、字體、Logo 與圖片素材仍為 `[TODO: 待品牌確認]`。首頁不得使用未確認的正式公司識別或客戶識別。

### 2.2 首頁區塊順序

| 順序 | 區塊 | 管理者要看到的內容 | 主要行動 |
|---:|---|---|---|
| 1 | 頂端導覽 | 校鮮集候選名稱、平台總覽、ERP、物流／取貨、LINE、FAQ | 進入團購平台 |
| 2 | Hero 首屏 | 100 校管理問題、品牌主張、展示資料標籤 | 進入團購平台 |
| 3 | 100 校營運總覽 | 學校數、進行中檔期、待處理訂單、待配送批次 | 查看管理 Dashboard |
| 4 | 管理價值三欄 | 校點、營運、例外三個核心價值 | 查看整合模組 |
| 5 | 多校管理 Dashboard | KPI、校別矩陣、例外佇列、訂單漏斗 | 查看校點／檔期 |
| 6 | 北部物流網絡 | 集貨中心、分區、校園交付點與批次 | 查看物流展示 |
| 7 | 三溫層取貨 | 加熱／保溫、常溫、冷藏設備分流 | 播放取貨模擬 |
| 8 | LINE 與訂單流程 | Order AI、Loop、StallPay 的產品邊界 | 查看 LINE 流程 |
| 9 | ERP 對帳與例外 | 訂單、收款、庫存、福利與批次核對 | 查看對帳畫面 |
| 10 | 最終 CTA | 再次說明平台入口與正式網址待確認 | 進入團購平台 |
| 11 | FAQ 與頁尾 | 展示邊界、正式名稱待確認、圖一格式主權頁尾 | 返回頂端／查看說明 |

## 三、首頁文案架構

### 3.1 Hero 首屏

**品牌眉標：**

> 校鮮集｜校園食品團購營運平台

**主標題候選：**

> **100 校團購，不必 100 份表格。**

**副標題：**

> 用 MerchCore.ai 的企業管理視角，集中查看學校、團購檔期、會員、商品、配送、取貨、庫存與對帳，讓管理者從單校訂單看見整體營運。

**展示標籤：**

> 100 校為展示數據｜正式品牌、平台網址與營運資料 `[TODO: 待人工確認]`

**主 CTA：**

> 進入團購平台

**次 CTA：**

> 查看企業管理 Dashboard

**首屏旁側畫面：**

建議使用一張「100 校營運總覽」視覺卡片。卡片內顯示北北基桃分區、校點數、進行中檔期、待配送批次與三溫層取貨狀態。所有數字旁須有「展示資料」標籤。

### 3.2 管理價值區

**區塊標題：**

> **把每一校的團購，放進同一條清楚的營運路徑。**

**三欄文案：**

| 標題 | 說明 |
|---|---|
| 多校看得見 | 以學區、學校與檔期整理訂單，不再從分散表格拼湊全貌。 |
| 批次接得上 | 從商品與溫層，到配送、取貨與設備狀態，保留每個交付節點。 |
| 例外先處理 | 缺貨、溫度、逾期未取、開門失敗與對帳差異，集中進入人工處理佇列。 |

### 3.3 Dashboard 區

**區塊標題：**

> **管理者先看總覽，再決定下一步。**

**區塊說明：**

> 從 100 校總覽進入學區、學校與團購檔期，查看訂單漏斗、配送批次、取貨點容量、溫層狀態與對帳差異。展示資料只用於說明介面結構，不代表正式營運報表。

**Dashboard 卡片文案：**

- 覆蓋學校：100 校｜展示數據
- 進行中團購檔期：24 檔｜展示數據
- 待處理訂單：1,286 筆｜展示數據
- 待配送批次：38 批｜展示數據
- 待取貨件：742 件｜展示數據
- 對帳待確認：6 筆｜展示數據

正式數字、校別名單、物流節點與客戶資料均為 `[TODO: 待人工確認]`。

### 3.4 物流與溫層區

**區塊標題：**

> **同一筆團購，依商品溫層走不同的取貨路徑。**

**區塊說明：**

> 管理者可以看到加熱／保溫、常溫與冷藏商品如何進入不同設備或交付點。模擬流程只呈現訂單、設備與狀態事件的關係；正式溫控標準、設備型號、開門協議與責任分工仍待確認。

**三張溫層卡片：**

| 溫層 | 展示標題 | 展示說明 | 取貨方式 |
|---|---|---|---|
| 加熱／保溫 | 熱食到站，確認保溫狀態 | 模擬設備確認加熱／保溫條件後，開放指定格位 | QR／取貨碼 → 狀態確認 → 開門 |
| 常溫 | 常溫商品，依批次分格 | 模擬商品批次與格位核對，避免領錯校點或訂單 | QR／取貨碼 → 格位核對 → 開門 |
| 冷藏 | 冷藏商品，先確認冷藏條件 | 模擬冷藏設備狀態、商品批次與取貨時段核對 | QR／取貨碼 → 冷藏狀態確認 → 開門 |

「加熱／保溫」是否為正式服務分類，以及加熱設備的溫度、時間與食品安全責任，均為 `[TODO: 待人工確認]`。展示文案不得宣稱食品已符合任何正式法規或保溫標準。

### 3.5 最終 CTA

**區塊標題：**

> **先進入團購平台，再一起確認正式服務範圍。**

**區塊說明：**

> 目前以「校鮮集」作為展示候選名稱，100 校與所有營運數字均為示意資料。正式品牌、團購平台網址、學校名單、物流商與設備接點，請以核准版本為準。

**主 CTA：**

> 進入團購平台

**CTA 備註：**

> 正式團購平台網址：`[TODO: 團購平台正式網址]`

## 四、三溫層無人取貨模擬設計

### 4.1 模擬定位

三溫層模擬的目標不是讓訪客誤認為無人設備已經正式接通，而是用一個可重播的管理情境，展示「訂單確認 → 溫層分流 → 設備核對 → 開門 → 取貨完成 → 稽核回寫」的資料關係。

模擬應使用 Adapter 邊界。網站不應直接寫入真實設備 API、開門 API、支付 API 或 LINE API。正式設備與 API 名稱、驗章方式、設備序號、格位編號、開門回應、失敗碼與維運責任均為 `[TODO: 待人工確認]`。

### 4.2 模擬畫面配置

建議將模擬畫面分為四個區域：

| 區域 | 內容 |
|---|---|
| 左側溫層選擇 | 加熱／保溫、常溫、冷藏三個模式按鈕 |
| 中央設備畫面 | 設備名稱、連線狀態、格位、商品批次與目前操作按鈕 |
| 右側事件串流 | 每個事件的目前／待處理／已完成狀態 |
| 下方安全提醒 | 顯示資料、API 未連接、開門失敗時不標記完成、溫控待確認 |

### 4.3 三溫層示意資料

| 欄位 | 加熱／保溫示意 | 常溫示意 | 冷藏示意 |
|---|---|---|---|
| 顯示名稱 | 加熱／保溫取貨格 | 常溫取貨格 | 冷藏取貨格 |
| 商品例 | 熱食餐盒／熟食組 `[TODO]` | 米麵／零食組 `[TODO]` | 鮮乳／水果組 `[TODO]` |
| 設備代號 | HEAT-01 `[TODO]` | AMBIENT-01 `[TODO]` | COLD-01 `[TODO]` |
| 格位 | H-03 `[TODO]` | A-12 `[TODO]` | C-07 `[TODO]` |
| 取貨驗證 | QR 或取貨碼 | QR 或取貨碼 | QR 或取貨碼 |
| 額外檢查 | 保溫／加熱狀態 | 批次與格位狀態 | 冷藏狀態與時段 |
| 失敗處理 | 不開門、不標記完成 | 不開門、不標記完成 | 不開門、不標記完成 |

以上設備代號、商品分類與格位均為介面示意，不是正式設備配置。

### 4.4 共用取貨狀態機

三個溫層共用一套展示狀態機，但各自保留不同的溫層檢查事件。

```text
待取貨
  → 取得 QR／取貨碼
  → 驗證訂單與會員範圍
  → 驗證校點與取貨批次
  → 驗證設備連線
  → 驗證溫層條件
  → 準備開門
  → 開門結果確認
  → 會員取出商品
  → 關門／設備回報
  → 取貨完成
  → 稽核紀錄回寫
```

任何事件出現逾時、簽章失敗、設備斷線、溫層條件不明、格位不一致或開門結果不明時，流程必須停在人工處理狀態。不得因前端動畫完成而自動標記取貨完成。

### 4.5 共用事件模型

| 順序 | 展示事件 | 說明 |
|---:|---|---|
| 01 | `pickup.code.received` | 取得 QR 或取貨碼 |
| 02 | `pickup.order.verified` | 驗證訂單、會員遮罩識別、校點與取貨批次 |
| 03 | `device.connection.checked` | 確認設備連線與設備 Adapter 狀態 |
| 04 | `temperature.condition.checked` | 確認該溫層的設備條件 |
| 05 | `locker.open.requested` | 透過設備 Adapter 送出開門請求；展示頁只模擬 |
| 06 | `locker.open.confirmed` | 收到明確開門結果；結果不明時停住 |
| 07 | `pickup.item.retrieved` | 模擬會員取出商品 |
| 08 | `locker.closed.reported` | 模擬設備回報關門 |
| 09 | `pickup.completed` | 條件完整後才標記取貨完成 |
| 10 | `audit.log.appended` | 寫入事件、操作者、設備、訂單與 UTC 稽核索引 |

正式事件名稱、payload、簽章與回應碼需由設備供應商與開發團隊確認。上述名稱是展示用 normalized event，不是對外 API 合約。

## 五、三種溫層的互動腳本

### 5.1 加熱／保溫取貨腳本

**畫面標題：**

> 加熱／保溫取貨｜先確認設備條件，再開放格位

**展示情境：**

一筆已確認的熱食團購訂單已送達指定取貨設備。系統先確認設備目前可提供的加熱／保溫狀態，再讓會員使用 QR 或取貨碼取貨。

**操作步驟：**

1. 點擊「加熱／保溫」。
2. 畫面顯示示意設備 `HEAT-01` 與格位 `H-03`。
3. 點擊「掃描 QR／輸入取貨碼」。
4. 顯示訂單、校點、取貨批次與商品溫層核對結果。
5. 點擊「模擬設備條件檢查」。
6. 事件 `temperature.condition.checked` 變為已完成。
7. 點擊「模擬開門」。
8. 顯示 `locker.open.requested` 與 `locker.open.confirmed`。
9. 點擊「模擬取出商品」。
10. 點擊「模擬關門回報」。
11. 事件 `pickup.completed` 與 `audit.log.appended` 才可完成。

**安全提示：**

> 展示頁不判定食品安全，也不代表加熱／保溫條件符合正式規範。實際溫度、時間、食品類別與責任 owner 為 `[TODO: 待人工確認]`。

### 5.2 常溫取貨腳本

**畫面標題：**

> 常溫取貨｜依校點、批次與格位核對後開門

**展示情境：**

一筆常溫食品訂單已分配到取貨格位。系統以訂單與格位資料核對，避免不同學校或不同團購檔期的商品被錯誤交付。

**操作步驟：**

1. 點擊「常溫」。
2. 畫面顯示示意設備 `AMBIENT-01` 與格位 `A-12`。
3. 點擊「掃描 QR／輸入取貨碼」。
4. 確認訂單、校點、商品批次與會員遮罩識別。
5. 點擊「模擬格位核對」。
6. 顯示格位可取與設備連線狀態。
7. 點擊「模擬開門」。
8. 點擊「模擬取出商品」與「模擬關門回報」。
9. 系統最後顯示取貨完成與稽核回寫。

**異常腳本：**

若模擬格位與訂單不一致，畫面應顯示「格位待人工核對」，不應開門，也不應更新為已完成。

### 5.3 冷藏取貨腳本

**畫面標題：**

> 冷藏取貨｜確認冷藏狀態與取貨時段

**展示情境：**

一筆冷藏食品訂單已送達冷藏設備。系統在開門前確認設備狀態、取貨時段、校點與商品批次。

**操作步驟：**

1. 點擊「冷藏」。
2. 畫面顯示示意設備 `COLD-01` 與格位 `C-07`。
3. 點擊「掃描 QR／輸入取貨碼」。
4. 顯示訂單、校點、商品批次與取貨時段。
5. 點擊「模擬冷藏條件檢查」。
6. 事件 `temperature.condition.checked` 變為已完成。
7. 若條件明確，點擊「模擬開門」。
8. 點擊「模擬取出商品」與「模擬關門回報」。
9. 顯示取貨完成、LINE 狀態通知概念與稽核回寫。

**異常腳本：**

若設備回報狀態不明、取貨時段已過、格位錯誤或冷藏條件無法確認，畫面應停在「待人工處理」，不得開門或標記完成。冷鏈溫度門檻、設備記錄頻率與食品責任界線為 `[TODO: 待人工確認]`。

## 六、Landing Page 與模擬畫面的串接

### 6.1 首頁入口

Hero 區的次 CTA 可使用「觀看三溫層取貨展示」。點擊後定位至物流／取貨區，並將模擬控制器預設在「常溫」模式，讓訪客先看到最容易理解的流程。畫面同時提供「切換加熱／保溫」與「切換冷藏」按鈕。

### 6.2 管理者簡報路徑

業務副總展示時，建議採用以下順序：

1. 在 Hero 說明「100 校團購，不必 100 份表格」。
2. 點擊「查看企業管理 Dashboard」，說明校點、訂單、批次與例外。
3. 點擊北部物流網絡，說明集貨中心、校園交付點與取貨點。
4. 以常溫模式播放一次完整取貨流程。
5. 切換冷藏，展示溫層條件檢查。
6. 切換加熱／保溫，展示額外設備條件與待確認邊界。
7. 點擊「進入團購平台」CTA。

這條路徑先回答管理規模，再回答交付方式，最後將訪客導向團購平台，不應先從設備規格開始。

### 6.3 互動狀態提示

| 狀態 | 建議文案 | 顏色語意 |
|---|---|---|
| 可繼續 | 條件已確認，可進入下一步 | 綠色 |
| 目前處理 | 正在等待設備回報 | 藍色 |
| 待人工 | 條件不明，請管理者確認 | 黃色 |
| 已完成 | 取貨事件已完成並寫入稽核展示 | 綠色 |
| 失敗 | 設備回報失敗，未標記取貨完成 | 紅色 |

所有狀態文字應清楚說明是「展示模擬」或「正式服務」。不可只使用綠色勾勾讓訪客推論真實設備已完成操作。

## 七、正式 API 邊界與 Adapter 規劃

### 7.1 建議的抽象介面

正式版本可由開發團隊評估以下平台中立介面。介面名稱是規劃用語，並非本展示頁的正式 API 合約。

| 介面 | 職責 |
|---|---|
| `IPickupDeviceProvider` | 設備註冊、連線狀態、能力查詢與設備健康度 |
| `ITemperatureProvider` | 溫層狀態讀取、條件檢查與異常回報 |
| `ILockerProvider` | 格位查詢、開門請求、開門結果與關門回報 |
| `IPickupVerificationProvider` | 訂單、會員遮罩識別、校點與取貨碼核對 |
| `INotificationProvider` | 取貨通知、異常通知與人工處理提醒 |
| `IAuditLogProvider` | 設備、訂單、取貨與人工處理事件留痕 |
| `IEventBus` | 訂單、設備、溫層、取貨與通知事件解耦 |

### 7.2 開門流程的最小資料欄位

正式 payload 仍需供應商確認。展示流程可先使用以下 normalized 欄位：

| 欄位 | 用途 | 展示處理 |
|---|---|---|
| `company_id` | 租戶範圍 | 使用合成識別，不放正式客戶資料 |
| `school_id` | 校點範圍 | 使用展示學校識別 |
| `order_id` | 訂單關聯 | 使用合成訂單編號 |
| `pickup_code_ref` | 取貨碼參照 | 不顯示完整敏感憑證 |
| `device_id` | 設備識別 | 使用示意設備代號 |
| `compartment_id` | 格位識別 | 使用示意格位編號 |
| `temperature_zone` | 溫層 | `heated`／`ambient`／`chilled` 需正式確認命名 |
| `idempotency_key` | 防止重複開門 | 展示事件使用唯一示意值 |
| `event_id` | 事件追蹤 | 每一步展示事件使用唯一值 |
| `occurred_at_utc` | UTC 時間 | 展示可顯示固定或模擬 UTC 時間 |
| `audit_ref` | 稽核索引 | 對應展示稽核事件 |

### 7.3 Fail-closed 原則

以下結果不可自動進入下一步：

- 設備連線逾時。
- 開門請求結果不明。
- 溫層條件無法取得。
- 訂單、校點與格位不一致。
- 取貨碼已使用或狀態衝突。
- API 簽章或設備憑證驗證失敗。
- 關門回報缺失。
- 事件重複或 `idempotency_key` 衝突。

展示頁應將這些情境呈現為「待人工處理」，並保留 `audit.log.appended` 概念。正式系統不得因畫面刷新、重複點擊或前端本地狀態而重複開門或重複完成取貨。

## 八、Landing Page 驗收清單

### 視覺與文案

- [ ] 首屏顯示「校鮮集」候選名稱與品牌候選標示。
- [ ] 首屏主標題面向企業管理者，而非只面向個別購買者。
- [ ] 主 CTA 為「進入團購平台」。
- [ ] 正式平台網址仍使用 `[TODO: 團購平台正式網址]`。
- [ ] 100 校、訂單、批次與物流數字均標示為展示資料。
- [ ] Dashboard、物流網絡、溫層取貨與對帳區塊均可由導覽進入。
- [ ] 頁尾沿用核准的 `© 2026 JCINN 捷州資訊. Concept showcase.` 及七項主權條款格式。

### 三溫層互動

- [ ] 可切換加熱／保溫、常溫與冷藏三種模式。
- [ ] 每種模式顯示不同設備、格位與商品溫層標籤。
- [ ] QR／取貨碼核對後才進入設備條件檢查。
- [ ] 加熱／保溫模式顯示條件檢查與待確認聲明。
- [ ] 常溫模式顯示校點、批次與格位核對。
- [ ] 冷藏模式顯示冷藏條件與取貨時段核對。
- [ ] 開門、取出、關門與稽核事件可依序播放。
- [ ] 失敗或結果不明時不標記取貨完成。
- [ ] 頁面明確說明設備 API 目前為展示模擬，不會實際開門。

## 九、首頁 SEO 與外部展示頁治理

首頁若要作為對外展示頁，SEO 應服務於「校園食品團購營運平台」與「多校團購管理」的搜尋理解，不應把尚未確認的 100 校、客戶名稱、營運成果或設備服務寫成正式事實。

### 9.1 搜尋與頁面標題

| 項目 | 草案內容 | 狀態 |
|---|---|---|
| Page title | 校鮮集｜校園食品團購營運平台｜多校管理展示 | 展示候選文案 |
| Meta description | 管理多校校園食品團購的企業平台概念，示範校點、檔期、商品、訂單、配送／取貨、三溫層設備與對帳流程。 | 展示候選文案 |
| H1 | 100 校團購，不必 100 份表格。 | 展示候選文案 |
| 主題詞 | 校園食品團購、多校團購管理、團購營運平台、校點配送、取貨設備、冷藏取貨 | 待 SEO 審查 |
| Canonical URL | `[TODO: 正式品牌網域]` | 待網域確認 |

正式採用前，應由品牌與 SEO 負責人確認商標、關鍵詞競爭、正式網域、語系 URL 與頁面索引策略。

### 9.2 建議的 SEO 內容層級

首頁應使用單一 H1，並以 H2 分隔管理者需求、100 校 Dashboard、北部物流網絡、三溫層取貨、LINE 互動與 ERP 對帳。每一個區塊都應提供可被搜尋引擎理解的文字說明，而不是只用圖表或動畫承載資訊。

三溫層模擬區塊的可索引文案可使用「加熱／保溫、常溫、冷藏取貨流程展示」，但必須同時標示「設備 API 展示模擬」與「正式溫控條件待確認」，避免搜尋摘要把概念模擬誤解為已提供的正式服務。

### 9.3 結構化資料與分享卡片

若正式網域與品牌名稱確認，頁面可評估加入 `Organization`、`WebSite`、`WebPage` 與 `FAQPage` 結構化資料。FAQ 結構化資料只可標記頁面上實際可見、且已核准的問答內容。Open Graph 與社群分享卡片應使用已授權的 Logo、品牌圖與正式頁面標題。

目前不得把 `[TODO: 待人工確認]` 內容放入正式結構化資料的品牌名稱、網址、電話、地址或服務承諾欄位。

### 9.4 內容與效能治理

首頁應避免以未壓縮大型圖片、外部追蹤碼或不必要的第三方資源拖慢首屏。所有展示數據應以可讀文字同步呈現，讓鍵盤操作、無障礙工具與搜尋引擎都能理解模組內容。互動模擬應提供明確的狀態文字，不應只依賴顏色、動畫或設備圖示。

外部展示頁仍需遵守平台中立、原始碼主權、外部化設定、最小權限、稽核可追溯、事件解耦、白標可擴展、全球合規與 AI 可替換要求。正式 AI、LINE、設備、支付、發票與物流服務均應透過可替換 Adapter，不在 Landing Page 內寫死供應商邏輯。

## 十、尚待確認事項

| 類別 | 待確認內容 |
|---|---|
| 品牌 | 正式公司名稱、商標、網域、Logo、品牌色與對外文案 |
| 平台 | 正式團購平台網址、登入方式、會員資料來源與服務區域 |
| 物流 | 學校清單、集貨中心、配送時窗、物流商與交付責任 |
| 溫層 | 加熱／保溫的正式定義、常溫與冷藏條件、食品安全責任 |
| 設備 | 無人機台與取物櫃型號、設備序號、格位模型、網路與韌體 |
| API | 開門 endpoint、驗章、簽章、錯誤碼、重試與冪等規則 |
| 通知 | LINE 官方帳號、接收者、訊息模板、異常升級與通知頻率 |
| 權限 | 總公司、區域、物流、校方與團購主可查看及操作的資料範圍 |
| 稽核 | UTC 時間、保存期限、PII 遮罩／加密與人工處理紀錄 |

## References

[1]: file:///home/ubuntu/upload/校園食品團購企業展示規劃v1.0 "校園食品團購企業展示規劃 v1.0"

[2]: https://feat-xiaoxianji-enterprise-p.stallpay-lp.pages.dev/?v=07 "校鮮集 Prototype v0.7 Cloudflare Branch Preview"

[3]: https://www.imin.com/product/falcon-1/ "iMin Falcon 1 官方產品頁"

[4]: https://oss-sg.imin.sg/docs/en/SDK.html "iMin 官方 SDK 文件"

[5]: https://oss-sg.imin.sg/docs/en/Printer.html "iMin 官方印表機文件"

本文件以 [1] 為主要需求來源，並以 [2] [3] [4] [5] 作為目前展示與候選設備邊界的參照。正式 API、設備、溫控、物流與營運條件均需由權責單位確認後，才能進入正式規格或實作。
