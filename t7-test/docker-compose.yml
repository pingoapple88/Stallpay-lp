services:
  t7-test-web:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    ports:
      - "${WEB_PORT:-8080}:8080"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
    security_opt:
      - no-new-privileges:true
